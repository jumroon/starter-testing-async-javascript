const axios = require("../utils/axios");
const BASE_URL = "http://localhost:5000";

const NAMES_URL = `${BASE_URL}/constellations`;

function getAllNames() {
  axios
}

function getConstellationsByQuadrant(quadrant) {}

module.exports = {
  getAllNames,
  getConstellationsByQuadrant,
};
